How to run the design:-

1. Open terminal (Press Ctrl + ~)
2. Enter command ./gradlew run --args="INPUT_FILE=jukebox-input.txt"

To debug:-

1. Open launch.json file from .vscode folder
2. Inside Configuration array, go inside object where mainClass is set as "com.crio.jukebox.App"
3. Click on Add Configuration and select args
4. Add this key-value pair inside this object "args": ["INPUT_FILE=jukebox-input.txt"]
5. Now come to App.java file and click on Debug option just above method name
6. You can find your abour runtime arguments on LHS, means it has been picked by our code 